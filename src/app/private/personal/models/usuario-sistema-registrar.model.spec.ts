import { UsuarioSistemaRegistrar } from './usuario-sistema-registrar.model';

describe('UsuarioSistemaRegistrar', () => {
  it('should create an instance', () => {
    expect(new UsuarioSistemaRegistrar()).toBeTruthy();
  });
});

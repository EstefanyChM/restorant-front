import { SuscripcionResponse } from './suscripcion-response.model';

describe('SuscripcionResponse', () => {
  it('should create an instance', () => {
    expect(new SuscripcionResponse()).toBeTruthy();
  });
});

import { SuscripcionRequest } from './suscripcion-request.model';

describe('SuscripcionRequest', () => {
  it('should create an instance', () => {
    expect(new SuscripcionRequest()).toBeTruthy();
  });
});

import { LegalCustomerRequest } from './legal-customer-request';

describe('LegalCustomerRequest', () => {
  it('should create an instance', () => {
    expect(new LegalCustomerRequest()).toBeTruthy();
  });
});

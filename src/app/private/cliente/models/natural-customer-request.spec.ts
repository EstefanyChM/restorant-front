import { NaturalCustomerRequest } from './natural-customer-request';

describe('NaturalCustomerRequest', () => {
  it('should create an instance', () => {
    expect(new NaturalCustomerRequest()).toBeTruthy();
  });
});

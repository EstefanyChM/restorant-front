import { LegalCustomerResponse } from './legal-customer-response';

describe('LegalCustomerResponse', () => {
  it('should create an instance', () => {
    expect(new LegalCustomerResponse()).toBeTruthy();
  });
});

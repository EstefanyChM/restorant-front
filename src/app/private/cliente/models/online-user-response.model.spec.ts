import { OnlineUserResponse } from './online-user-response.model';

describe('OnlineUserResponse', () => {
  it('should create an instance', () => {
    expect(new OnlineUserResponse()).toBeTruthy();
  });
});

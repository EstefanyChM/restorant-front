import { EnTiendaResponse } from './en-tienda-response.model';

describe('EnTiendaResponse', () => {
  it('should create an instance', () => {
    expect(new EnTiendaResponse()).toBeTruthy();
  });
});

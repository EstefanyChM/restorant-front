import { OnlineUserRegistoRequest } from './online-user-registo-request.model';

describe('OnlineUserRegistoRequest', () => {
  it('should create an instance', () => {
    expect(new OnlineUserRegistoRequest()).toBeTruthy();
  });
});
